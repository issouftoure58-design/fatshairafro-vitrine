## Packages
framer-motion | Smooth message entry animations and loading states
react-markdown | Rendering formatted AI responses
date-fns | Timestamp formatting

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  mono: ["var(--font-mono)"],
}
